apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trading

resources:
  - namespace.yaml
  - credentials-secret.yaml
  - clickhouse.yaml
  - connector-configmap.yaml
  - deployment.yaml
  - service.yaml
  # Apply schema-job.yaml manually once: kubectl apply -f deploy/k8s/schema-job.yaml
